#include<iostream>
#include<string>
using namespace std;
int summation(int x, int y);
int main()
{
	int result;
	result=summation(5,10);
	cout<<result<<endl;
	return(0);
}
int summation(int x,int y) //x, y to receieve line 8 in same position
{
	int sum=x*y;
	return sum; //return value to calling position
}

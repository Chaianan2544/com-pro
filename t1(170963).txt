#include<iostream>
#include<string>
using namespace std;
int summation(int x, int y);
int main()
{
	int result;
	result=summation(5,10);
	cout<<result<<endl;
	return(0);
}
int summation(int x,int y)
{
	int sum=x*y;
	return sum;
}